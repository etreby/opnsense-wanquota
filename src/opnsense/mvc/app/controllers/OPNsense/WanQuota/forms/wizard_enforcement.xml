<?xml version="1.0" ?>
<form>
    <field>
        <id>wanquota.general.enforcement_enabled</id>
        <label>Enable automatic guardrail enforcement</label>
        <type>checkbox</type>
        <help>Requires dry-run to be disabled and a non-observe policy. Leave disabled until recommendations are verified.</help>
    </field>
    <field>
        <id>wanquota.general.enforcement_dry_run</id>
        <label>Guardrail dry-run mode</label>
        <type>checkbox</type>
        <help>Recommend without acting. Keep this on while validating; every recommendation is recorded but nothing is changed.</help>
    </field>
    <field>
        <id>wanquota.general.device_enforcement_enabled</id>
        <label>Per-device budget enforcement</label>
        <type>checkbox</type>
        <help>Block individual devices that exceed their configured budget, by maintaining the wanquota_over_budget firewall table. Requires a firewall rule that blocks that table. The router, devices marked excluded, and members of a protected infrastructure group are never blocked.</help>
    </field>
    <field>
        <id>wanquota.general.device_enforcement_dry_run</id>
        <label>Per-device dry-run mode</label>
        <type>checkbox</type>
        <help>Record which devices would be blocked without changing the firewall table. Leave enabled until the recorded membership matches what you intend to block.</help>
    </field>
    <field>
        <id>wanquota.general.shaper_upload_experimental</id>
        <label>Experimental upload shaping (layer2)</label>
        <type>checkbox</type>
        <help>Only useful where a netmap packet-capture engine such as Zenarmor hides LAN egress from the normal firewall hook, which otherwise makes an upload cap impossible. Measured to work: a layer2 rule counted 3,005 packets of a device's uploads where the same match at the IP layer counted 3. Experimental because the traffic shaper model has no layer2 field, so these are raw ipfw rules that any firewall or shaper reload removes until the five-minute collector puts them back, and because enabling it turns on layer2 filtering globally. A permit-everything rule is installed first so what passes does not change. Use Verify to confirm the upload rule is matching.</help>
    </field>
    <field>
        <id>wanquota.general.enforcement_policy</id>
        <label>Maximum automatic policy</label>
        <type>dropdown</type>
        <help>The strongest action the guardrails may take: observe, deprioritize, fail over, or cut off.</help>
    </field>
    <field>
        <id>wanquota.general.guardrail_thresholds</id>
        <label>Guardrail thresholds (%)</label>
        <type>text</type>
        <help>Four ascending percentages at which the next stronger action is recommended, for example 50,75,90,100.</help>
    </field>
    <field>
        <id>wanquota.general.emergency_reserve_gb</id>
        <label>Emergency reserve (GB)</label>
        <type>text</type>
        <help>Hold back this much of the allowance. The strongest action is only recommended once the remainder falls below it.</help>
    </field>
</form>
